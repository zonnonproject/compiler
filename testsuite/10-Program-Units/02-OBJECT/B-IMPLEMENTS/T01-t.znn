(** 
 * 11. Program Units.
 * 11.2. object
 * implements clause
 *
 * Positive test: the object A uses definition name B in implements.
 * The definition B is used before it is declared.
 *)
object {public} A implements B;
end A.

definition {public} B;
end B.

module Main;
begin
	halt(1);
end Main.