(** 
 * 11. Program Units.
 * 11.2. object
 * Object name
 *
 * Negative test: wrong object name.
 * The nested object has qualified name C.D.A.
 *
 * Correct diagnostic must be output for wrong names C.D.A.
 *)
module C;
	object C.D.A;
	end A;
end C.

module Main;
begin
	halt(0);
end Main.