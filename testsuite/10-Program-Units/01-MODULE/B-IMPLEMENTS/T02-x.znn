(** 
 * 11.Program Units.
 * 11.1. module
 * implements clause
 *
 * Negative test: wrong definition name B.C is used in implements clause.
 * The name B.C is type name. It is declared before it is used.
 * 	
 * Correct diagnostic must be output for wrong name B.C!
 *)
module B;
type { public } 
	C = ( one, two );
end B.

module A implements B.C;
end A.

module Main;
begin
	halt(0);
end Main.