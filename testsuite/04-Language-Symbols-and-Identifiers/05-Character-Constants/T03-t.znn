(** 
 * 4. Language symbols and identifiers. 
 * 4.5. Character Constants
 *
 * Positive test: compare two character with different presentation.
 *)
module Main;
begin
	if ' ' = 20X
	then halt(1) else halt(0) end;
end Main.

