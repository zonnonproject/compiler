(**
 * 09.Predefined procedures.
 * The max(T) procedure.
 *
 * Positive test: max value for enumeration type.
 *)
module Main;
	type E = ( one, two, three );
	
	var e : E;
begin
	e := max(E);

	if e = E.three 
	then halt(1) else halt(0) end;
end Main.