(**
 * 09.Predefined procedures.
 * The max(T) procedure.
 *
 * Positive test: max value for set type.
 *)
module Main;
	var i : integer;
begin
	i := max( set{8} );

	if i = 7 
	then halt(1) else halt(0) end;
end Main.
