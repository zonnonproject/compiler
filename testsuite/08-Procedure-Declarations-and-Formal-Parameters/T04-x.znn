(** 
 * 8. Procedure declaration and formal parameters
 *
 * Negative test: the function call uses wrong parameters count.
 * The count is more then 1.
 *)
module Main;
	procedure A(i : integer) : integer;
	begin
	end A;
begin
	A(1, 2);
	halt(0);
end Main.