(** 
 * 8. Procedure declaration and formal parameters
 *
 * Negative test: the procedure A does not return any result.
 *)
module Main;
	procedure A : integer;
	begin
		return;
	end A;
begin
	A;
	halt(0);
end Main.