(** 
 * 8. Procedure declaration and formal parameters
 *
 * Negative test: the procedure A uses not initilezed variable in return construct.
 *)
module Main;
	procedure A : integer;
		var k : integer;
	begin
		return k + 1;
	end A;
begin
	A;
	halt(0);
end Main.