(** 
 * 7. Statement 
 * 7.7. FOR statement.
 *
 * Positive test: for statement has integer control variable.
 *)
module Main;
	var	s, i : integer;
begin
	s := 0;

	for i := 1 to 10 do;
		s := i;
	end;

	if s = 10 then halt(1) else halt(0) end;
end Main.