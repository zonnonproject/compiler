(** 
 * 17. Math extensions.
 * 17.3. Indexing
 * 
 * Negative test: integer vector indexing (array is not math)
 *)

module Main;

type v = array * of integer;

var
	b : v;
	a : array 5 of real;
begin	
	b := [1, 3];
	a[b] := [-1., -2.];
	
	halt(0);
end Main.