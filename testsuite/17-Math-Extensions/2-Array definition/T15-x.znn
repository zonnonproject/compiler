(** 
 * 17. Math extensions.
 * 17.2. Definition
 * 
 * Negaive test: dynamic math array creation & expression array definition:
 * incorrect dimension (belittling)
 *)

module Main;
var
	a : array {math} *, * of real;
begin
	a := [1., 2., 3.];
	halt(0);
end Main.