(** 
 * 17. Math extensions.
 * 17.2. Definition
 * 
 * Positive test: dynamic math array creation.
 *)
module Main;

type v = array {math} * of integer;

var
	a : v;
begin	
	a := new v(2);
	a[0] := 5;
	halt(1);
end Main.