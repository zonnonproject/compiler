(** 
 * 6. Expression.
 * 6.1. Operands
 * A. Instance = self
 *
 * Negative test: access to variable that does not exist.
 *)
object A;
	var i : integer;
begin
	self.x := 1;
	i := self.x;
end A.

module Main;
begin	
	halt(0);
end Main.