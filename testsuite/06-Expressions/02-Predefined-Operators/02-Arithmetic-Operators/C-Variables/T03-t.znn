(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.3. Arithmetic Operators
 *
 * Positive test: 5 * 2 with variables
 *)
module Main;
	var a, b : integer;
begin
	a := 5;
	b := 2;

	if a * b = 10 
	then halt(1) else halt(0) end;
end Main.