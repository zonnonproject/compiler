(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.5. Relation Operators.
 *
 * Positive test: = operator for integer variables.
 *)
module Main;
	var x, y : real;
begin
	x := 1.0;
	y := 1.0;

	if y = x 
	then halt(1) else halt(0) end;
end Main.