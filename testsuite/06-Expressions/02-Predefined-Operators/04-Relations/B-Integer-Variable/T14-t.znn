(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.5. Relation Operators.
 *
 * Positive test: # operator for integervariables.
 *)
module Main;
	var x, y : real;
begin
	x := real(2);
	y := real(2);

	if y # x 
	then halt(0) else halt(1) end;
end Main.