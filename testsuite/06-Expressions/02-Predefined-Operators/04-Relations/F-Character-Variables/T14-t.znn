(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.5. Relation Operators.
 *
 * Positive test: < operator for character variables.
 *)
module Main;
	var c1, c2 : char;
begin
	c1 := '0';
	c2 := '1';

	if c1 # c2 
	then halt(1) else halt(0) end;
end Main.