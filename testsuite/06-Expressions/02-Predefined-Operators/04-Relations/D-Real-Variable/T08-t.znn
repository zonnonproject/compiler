(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.5. Relation Operators.
 *
 * Positive test: <= operator for real variables.
 *)
module Main;
	var x, y : real;
begin
	x := 1.5;
	y := 2.6;

	if x <= x 
	then halt(1) else halt(0) end;
end Main.