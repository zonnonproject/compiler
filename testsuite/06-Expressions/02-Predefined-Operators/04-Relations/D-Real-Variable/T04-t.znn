(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.5. Relation Operators.
 *
 * Positive test: > operator.
 *)
module Main;
	var x, y : real;
begin
	x := 1.0;
	y := 2.0;

	if x > y 
	then halt(0) else halt(1) end;
end Main.