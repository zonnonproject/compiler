(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.5. Relation Operators.
 *
 * Positive test: <= operator.
 *)
module Main;
	var x, y : real;
begin
	x := 1.111;
	y := 2.222;

	if x <= y 
	then halt(1) else halt(0) end;
end Main.