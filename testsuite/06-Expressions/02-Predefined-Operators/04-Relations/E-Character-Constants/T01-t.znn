(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.5. Relation Operators.
 *
 * Positive test: < operator for character constants.
 *)
module Main;
begin
	if '0' < '1' 
	then halt(1) else halt(0) end;
end Main.