(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.4. Set Operators.
 *
 * Negative test: not set is used in in operator.
 *)
module Main;
	import A;

	var s : set;
begin
	s := {1, 2, 3};

	if (2.0 in s)
	then halt(0) else halt(1) end;
end Main.

object A.B;
end B.
