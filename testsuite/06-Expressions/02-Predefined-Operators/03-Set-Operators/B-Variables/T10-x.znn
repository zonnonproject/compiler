(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.4. Set Operators.
 *
 * Negative test: real element is not correct in sets.
 *)
module Main;
	var 
		s : set;
		r : real;
begin
	r := 2.0;
	s := { 1, r, 3 };

	if  (r in s)
	then halt(1) else halt(0) end;
end Main.