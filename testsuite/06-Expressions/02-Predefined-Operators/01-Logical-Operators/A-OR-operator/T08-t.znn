(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.2. Logical Operators
 * A. or operator.
 *
 * Positive test: false or false for boolean variables.
 *)
module Main;
	var p, q : boolean;
begin
	p := false;
	q := false;

	if p or q 
	then halt(0) else halt(1) end;
end Main.