(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.2. Logical Operators.
 * C. ~ (negation) operator.
 *
 * Positive test: ~ for boolean variable false value.
 *)
module Main;
	var b : boolean;
begin
	b := false;
	if ~b 
	then halt(1) else halt(0) end;
end Main.
