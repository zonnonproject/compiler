(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.2. Logical Operators.
 * C. ~ (negation) operator.
 *
 * Positive test: ~ true
 *)
module Main;
begin
	if ~true 
	then halt(0) else halt(1) end;
end Main.