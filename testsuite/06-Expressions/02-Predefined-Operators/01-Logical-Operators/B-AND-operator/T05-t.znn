(** 
 * 6. Expression.
 * 6.2. Predefined Operators.
 * 6.2.2. Logical Operators
 * B. AND operator.
 *
 * Positive test: true & true for boolean variables.
 *)
module Main;
	var p, q : boolean;
begin
	p := true;
	q := true;

	if p & q 
	then halt(1) else halt(0) end;
end Main.