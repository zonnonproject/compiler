(**
 * 5. Declarations.
 * 5.3. Type declaration.
 * 6.3.4. String type.
 *
 * Positive test: string constants comparison 
 *)
module Main;
begin 
	if "ABC" <= "AB"
	then halt(0) else halt(1) end;
end Main.