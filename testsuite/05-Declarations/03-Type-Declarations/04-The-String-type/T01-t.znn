(**
 * 5. Declarations.
 * 5.3. Type declaration.
 * 6.3.4. String type.
 *
 * Positive test: string type is type from underlined environment.
 *)
module Main;

import System.String as String;

var s : String;

begin
	s := "abc";

	if s.StartsWith("ab")
	then halt(1) else halt(0) end;
end Main.

