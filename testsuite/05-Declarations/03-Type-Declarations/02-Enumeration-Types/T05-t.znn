(** 
 * 5. Declarations.
 * 5.3. Type declaration.
 * 5.3.2. Enumeration types.
 *
 * Positive test: predefined procedure val is used with enumeration values.
 *)
module Main;
type
	NumberKind = ( Bin, Oct, Dec, Hex );
begin
	if integer(NumberKind.Oct) = 1 
	then halt(1) else halt(0) end;
end Main.