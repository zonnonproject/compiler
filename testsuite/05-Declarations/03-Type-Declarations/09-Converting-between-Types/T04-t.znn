(** 
 * 5. Declarations.
 * 5.3. Type declaration.
 * 5.3.8. Casting between types
 *
 * Positive test: casting from integer to real.
 *)
module Main;
var 
	i : integer;
	r : real;
begin
	i := 10;
	r := real(i);

	if r = 10.0
	then halt(1) else halt(0) end;
end Main.