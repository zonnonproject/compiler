(** 
 * 5. Declarations.
 * 5.3. Type declaration.
 * 5.3.8. Casting between types
 *
 * Positive test: casting from character literal to integer.
 *)
module Main;
	var i, j : integer;
begin
	i := integer(20X); (* character literal *)

	if i = 32 
	then halt(1) else halt(0) end;
end Main.
