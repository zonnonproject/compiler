(** 
 * 5. Declarations.
 * 5.3. Type declaration.
 * 5.3.8. Casting between types
 *
 * Positive test: casting from real to integer.
 *)
module Main;
var 
	i : integer;
	r : real;
begin
	r := 10.1;
	i := integer(r);

	if i = 10
	then halt(1) else halt(0) end;
end Main.
