(** 
 * 5. Declarations.
 * 5.3. Type declaration.
 * 5.3.8. Casting between types
 *
 * Positive test: casting from character to integer.
 *)
module Main;
	var i, j : integer;
begin
	i := integer('a');
	j := integer('b');

	if i = (j-1) 
	then halt(1) else halt(0) end;
end Main.
