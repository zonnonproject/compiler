(** 
 * 5. Declarations.
 * 5.3. Type declaration.
 * 5.3.1. Basic types.
 * A-boolean
 *
 * Positive test: true value assignment for boolean variable.	
 *)
module Main;
var 
	b : boolean;
begin
	b := true;

	if b 
	then halt(1) else halt(0) end;
end Main.
