(** 
 * 5. Declarations.
 * 5.3. Type declaration.
 * 5.3.1. Basic types.
 * set type
 *
 * Positive test: set value assignment for set variable.
 *)
module Main;
	var s : set ;
begin
	s := { 1 };

	if 1 in s 
	then halt(1) else halt(0) end;
end Main.
