(** 
 * 5. Declarations.
 * 5.3. Type declaration.
 * 5.3.4. String type.
 *
 * Positive test: the array length is constant.
 * The constant is declated after the array declaration.
 *)
module A;
	type T = array N of real;

	const N = 100;
end A.

module Main;
begin
	halt(1);
end Main.